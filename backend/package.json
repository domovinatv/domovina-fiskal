{
  "name": "domovina-fiskal-backend",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply fiskal_domovina --local",
    "db:migrate:prod": "wrangler d1 migrations apply fiskal_domovina --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "bwip-js": "^4.11.2",
    "hono": "^4.6.14",
    "pdf-lib": "^1.17.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
